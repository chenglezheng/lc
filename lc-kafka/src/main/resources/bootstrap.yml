spring:
  cloud:
    config:
      discovery:
        service-id: lc-config-server
        enabled: true
      label: master
      enabled: true
      fail-fast: true
      profile: fjy
  rabbitmq:
    port: 5672
    host: localhost
    username: guest
    password: guest
