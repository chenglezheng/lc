server:
  port: 10000
spring:
  application:
    name: lc-config-client
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: lc-config-server
      enabled: true
      fail-fast: true
      profile: dev
